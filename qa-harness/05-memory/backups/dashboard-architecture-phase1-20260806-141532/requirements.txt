# Dashboard backend dependencies
# Install with:  pip install -r dashboard/backend/requirements.txt

fastapi>=0.115.0       # Web framework - handles API routes and WebSocket
uvicorn[standard]>=0.30.0   # ASGI server - runs the FastAPI app
sqlalchemy>=2.0.0      # ORM - maps Python classes to SQLite tables
websockets>=12.0       # WebSocket protocol support for live log streaming
httpx>=0.27.0          # Dashboard-backend HTTP client for observability services, not for Playwright test requests
locust>=2.32.0         # Load-testing runner invoked by the performance dashboard
prometheus-client>=0.20.0  # Expose Locust metrics for Prometheus scraping
openpyxl>=3.1.0       # Generate West Kowloon story-scoped xlsx test-case files
