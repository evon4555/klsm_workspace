# 需求整合 — <scope name>

| 项目 | 内容 |
|----|----|
| 包 | `<module>/<yyyy-mm-dd>` |
| 范围 | `<short slug>` |
| 当前状态 | 待确认 / 已确认 / 已废弃 |
| 签字规则 | 测试经理只填写本文档末尾问题表的「答复 / 确认」列，并在修订历史中确认；AI 不自行签字 |

## 修订历史

| 日期 | 作者 | 修改内容 | 状态 |
|----|----|----|----|
| YYYY-MM-DD | QA1 (AI) | 初版，依据 `01-input/` 整理范围 | 已完成 |
| YYYY-MM-DD | 测试经理 | 待填写：确认范围并答复 Q / U 表 | 待确认 |

## 1. 有没有问题？

先给结论，说明当前是否可以进入 `03-test-design`。

- 如果仍有待决策项：写明 `有 — 当前还不能进入 03-test-design`。
- 如果已经全部答复并签署：写明 `无阻塞 — 可以进入 03-test-design`。
- 列出需要答复的决策数量，例如 `Q-1..Q-5`。
- 列出需要确认的假设数量，例如 `U-1..U-5`。

## 2. 问题分类

| 类型 | 编号区间 | 数量 | 决策人 | 当前状态 |
|----|----|----|----|----|
| 范围决策 | Q-1..Q-n | n | 测试经理 / 产品 / 开发按需参与 | 待答复 |
| 假设确认 | U-1..U-n | n | 测试经理确认，必要时开发补充 | 待确认 |

> 决策详细清单放在文档末尾。测试经理只需要填写「答复 / 确认」列。

## 3. 测试范围

### 3.1 范围内

| 编号 | 能力 | 说明 |
|----|----|----|
| F1 | <能力名称> | <可以直接转成测试点的业务说明> |
| F2 | <能力名称> | <可以直接转成测试点的业务说明> |

### 3.2 范围外 — 推迟

| 内容 | 推迟原因 | 后续位置 |
|----|----|----|
| <内容> | <原因> | <目标日期包或 TBD> |

### 3.3 范围外 — 由其他模块负责

| 内容 | 责任方 | 说明 |
|----|----|----|
| <内容> | <模块 / 团队> | <为什么不在本包做> |

## 4. 差异项

### 4.1 与上一版本对比

| 编号 | 旧行为 | 新行为 | 测试影响 |
|----|----|----|----|
| D-prev-1 | <旧行为> | <新行为> | <是否需要新增 / 修改 / 删除用例> |

### 4.2 与标准产品对比

| 编号 | 标准产品行为 | 本项目 / 本包行为 | 是否回流标准产品 |
|----|----|----|----|
| D-std-1 | <标准产品行为> | <本项目行为> | 是 / 否 / 不适用 / 待定 |

### 4.3 与相邻模块影响

| 编号 | 受影响模块 | 影响方式 |
|----|----|----|
| D-adj-1 | <模块> | <影响说明> |

## 5. 来源

| 来源 | 类型 | 是否权威 | 备注 |
|----|----|----|----|
| `<path>` | PRD / 禅道 / Figma / 脑图 / 上一版本用例 | 是 / 参考 | <说明> |

---

## 6. 问题清单（决策与确认）

读到这里说明上文已读完。下方表格是本次评审需要拍板的内容。

填写规则：测试经理只填最后一列，写 `a` / `b` / `c`，或写一句简短修正。

### 6.1 需要决策（Q-1..Q-n）

| # | 决策点 | 选项 | 默认 | 答复 |
|----|----|----|----|----|
| Q-1 | <决策点> | (a) <推荐选项>；(b) <选项>；(c) <选项> | (a) |  |

### 6.2 需要确认的假设（U-1..U-n）

| # | 模糊点 | AI 暂定假设 | 确认 / 修正 |
|----|----|----|----|
| U-1 | <源材料没有说清楚的点> | <AI 默认假设> |  |

## 7. 下一步

- 测试经理填写 § 6.1 的「答复」列和 § 6.2 的「确认 / 修正」列。
- AI 根据答复更新本文档状态、`package.md` 状态和后续用例设计范围。
- 在本文档确认前，不创建 `03-test-design` 用例。

## 模板约束

- 本文件是需求整理工作界面的唯一模板入口；不要把某个历史日期包的
  具体文档当作模板引用。
- 用户-facing 的需求整理文档不得出现 `[SECTION]` 标记。
- 不使用 `Integrated Breakdown`、`Hand-Off Statement` 这类内部模板标题。
- 不要求测试经理填写 `Review Findings`、`Open Review Comments`、`Resolution / Status` 或流程状态字段。
- 对相似需求变更，也按新需求完整走需求评估和用例评审；区别是必须参考上一版本测试用例和截图。
- 对需求变更产生的用例改动，后续用例文件中变更部分必须黄色 highlight。
