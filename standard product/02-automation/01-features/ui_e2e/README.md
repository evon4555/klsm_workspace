# UI E2E Features

UI scenarios must use Playwright against the real Standard Product application.

No local HTML fixture can be counted as UI automation execution.

No runnable Batch Session Configuration UI scenario exists yet.
