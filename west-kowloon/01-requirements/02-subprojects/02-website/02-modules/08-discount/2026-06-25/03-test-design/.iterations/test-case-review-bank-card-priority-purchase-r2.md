# AI 评审 r2 — 银行卡优先购 用例 v2

| 项目 | 值 |
|---|---|
| 评审对象 | v2 = v1 (45 条) + 3 条新增 (BPP-046..048) = 48 条 |
| 评审范围 | **仅 v2 新增 3 条 + 触发的 r1 残余风险复核**（v1 原 45 条不复评） |
| 评审人 | QA1 (AI) |
| 日期 | 2026-06-29 |
| 触发 | 04 form v1 签 Pass with revisions，Q-2/Q-3/Q-5 = a (Q-3/Q-4 用户口头调整) |

---

## 1. 总结论

**Pass with 2 residual risk notes** — 3 条新增覆盖了原 r1 Medium gap (BPP-046/047/048)，无新阻塞；2 个残余风险需要联调或运行期决定，**用例侧无法预先验证**，已在 Expected Result 中明示「以实际实现为准」。

| 严重度 | 数量 | 编号 |
|---|---|---|
| Critical | 0 | — |
| High | 0 | — |
| Medium | 0 | — |
| Minor | 2 (info only) | RR-1 / RR-2 |

---

## 2. 新增用例自评 (BPP-046..048)

### BPP-046 — 凭证过期边界

| 维度 | 评分 | 说明 |
|---|---|---|
| 覆盖度 | 强 | 三种边界（剩 0 天 / 凌晨跨日 / 失效瞬间）+ 跨日 UI 闪烁断言 |
| 可执行性 | 强 | mock 凭证失效时间 + mock 当前时间，SIT 都支持 |
| Expected 清晰度 | 强 | 4 条断言对齐 4 步，规则「失效时刻 ≤ 当前 = 已过期」明确 |
| 风险 | 无 | 与 BPP-014 / BPP-015 不重复（BPP-014 是已过期场景；BPP-046 是边界）|

### BPP-047 — 30 分窗口模糊边界

| 维度 | 评分 | 说明 |
|---|---|---|
| 覆盖度 | 中 | 仅 29 / 31 / ≈ 30 三点，按 Q-3(c) 决策不验证秒级 |
| 可执行性 | 强 | mock 当前时间即可 |
| Expected 清晰度 | 强 | 显式说明「≈ 30 分边界命中或不命中均可」，避免歧义 |
| 风险 | **RR-1**（见 § 3） | 精确开/闭区间未定，将来如果 PRD 补充再开 U 回流 consolidation |

### BPP-048 — 次数耗尽

| 维度 | 评分 | 说明 |
|---|---|---|
| 覆盖度 | 强 | 覆盖 = 0 时的降级 + 不打断 + 弱提示 |
| 可执行性 | 强 | mock 次数即可，SIT 应支持 |
| Expected 清晰度 | 中 | 弱提示文案写「具体以实际实现为准」(RR-2) |
| 风险 | **RR-2**（见 § 3） | 弱提示文案是用 F8 通用样式还是单独文案 PRD 没说 |

---

## 3. 残余风险（info-only，不阻塞执行）

| ID | 风险描述 | 严重度 | 处理建议 |
|---|---|---|---|
| RR-1 | BPP-047 窗口边界精确开/闭区间未定。Expected 写「命中或不命中均可」，等执行时联调以实际实现为准 | Info | 执行时若联调发现实现明确（如严格 ≤ 30:00），update 用例为精确断言；如出现争议再回 consolidation 开 U |
| RR-2 | BPP-048 次数耗尽弱提示具体文案 PRD 无规定 | Info | 执行时按实际显示填入，必要时与 PM 确认是否需单独 i18n |

---

## 4. 跳过项审计（用户口头改 04 决策）

| 04 已签 | v2 实际操作 | 我的复核意见 |
|---|---|---|
| Q-3 = a (补精确边界) → 改为 c | 我同意：精确边界的 PASS/FAIL 判定本就不稳，留模糊更可执行 | ✅ |
| Q-4 = a (补等值最优) → 跳过 | 我同意：tiebreak 本质是后端 DB 排序决定，用户无可观测差异（任何选择都"合理"），属于"非用户验证"行为；当前 BPP-024 多绑定取最优只断"显示最优总价"，没断"哪一个"，已隐含覆盖 | ✅ |

— 两项调整不影响 v2 通过。

---

## 5. v1 r1 评审残余项复核

r1 共 8 项建议（4 Medium + 4 Minor），04 form 决策结果：

| r1 编号 | 04 决策 | v2 实际 | 状态 |
|---|---|---|---|
| BPP-046 凭证 0 天 / 凌晨过期 | a 补 | ✅ 补 BPP-046 | 已消化 |
| BPP-047 30 分窗口边界 | a 补 | ✅ 补 BPP-047（按 Q-3=c 模糊化）| 已消化 |
| BPP-048 等值最优 | a 补 | ❌ 跳过 | 用户口头改 — audit 记录在 CHANGE.md |
| BPP-049 次数耗尽 | a 补 | ✅ 补 BPP-048 | 已消化（编号调整为 048）|
| BPP-050 弱提示频次 | b 跳过 | 跳过 | 一致 |
| BPP-051 卡号粘贴去空格 | b 跳过 | 跳过 | 一致 |
| BPP-052 重复绑定同一 BIN | b 跳过 | 跳过 | 一致 |
| BPP-053 E2E 失败重试 | b 跳过 | 跳过 | 一致 |

**消化率**：3 / 4 补项已实现；1 项跳过 (audit 记录)。

---

## 6. 评审建议给测试经理

- 整体 **Pass** — v2 可签字进 05-execution
- 两个 RR 不阻塞但提醒执行人留意，执行回填时若发现实际行为与 Expected 偏离，记入用例 Actual Result 列
- BPP-046..048 EN 译本已在 v2 同步生成，建议签 v2 之前先看一眼 EN xlsx 的对应 3 行
- 下次 04 评审若想跳过某项 r1 建议，最好直接在 04 form 答 b（而不是签 a 后口头改），避免 audit trail 分裂在两处
