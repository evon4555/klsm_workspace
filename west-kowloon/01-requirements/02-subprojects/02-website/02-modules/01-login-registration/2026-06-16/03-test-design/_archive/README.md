# Test Design Archive

This archive preserves historical artifacts from the 2026-06-16
login/registration test-design package.

- `scripts`: one-off migration, cleanup, validation, and writeback helpers.
- `workbooks`: older workbook versions and backup copies.
- `reports`: old diffs, maintenance reports, and supporting notes.
- `iterations`: earlier review and iteration snapshots.

Use the root of `03-test-design` for current package deliverables. Use this
archive only when investigating how a workbook or review result was produced.
